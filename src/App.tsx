function App() {
  return (
    <main id="app">
      <h1>Storybook Lab</h1>
      <p>
        This app has no real UI of its own &mdash; it exists to host components
        for Storybook. Run <code>yarn storybook</code> and open{" "}
        <a href="http://localhost:6006">localhost:6006</a> to browse them.
      </p>
    </main>
  );
}

export default App;
